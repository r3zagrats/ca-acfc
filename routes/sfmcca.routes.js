// 'use strict';

// const router = require('express').Router();
// const SFMCCAController = require('../controllers/sfmcca.controllers');

// router.use('journey/execute', SFMCCAController.execute);
// router.use('journey/save', SFMCCAController.save);
// router.use('journey/publish', SFMCCAController.publish);
// router.use('journey/validate', SFMCCAController.validate);
// router.use('journey/stop', SFMCCAController.stop);

// module.exports = router;
